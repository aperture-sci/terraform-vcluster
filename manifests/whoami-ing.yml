apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
 name: whoami
 namespace: whoami
 annotations:
   cert-manager.io/issuer: "le-example-http"
spec:
 tls:
   - hosts:
       - whoami.kubecon.sales-dev.codefresh.io
     secretName: tls-whoami-ingress-http
 rules:
   - host: whoami.kubecon.sales-dev.codefresh.io
     http:
       paths:
         - path: /
           pathType: Prefix
           backend:
             service:
               name: whoami
               port:
                 name: web
